import java.util.ArrayList;
import java.util.Date;

public class Main {
    public static void main(String[] args) {
        // 자바에서 패키지란 (폴더)
        // 패키지는 비슷한 기능의 클래스의(배포하면 Lib or app(독자적으로 실행되는 것)이 된다.) 집합
        // 자바에서 패키지는 클래스 이름의 일부(클래스 이름의 충돌을 막는다)

        // 보통 패키지를 프로젝트의 도메인 주소(web 상에서 유일한 주소)를 사용. => 클래스 이름을 유일하게 만들어준다.

//        java.util.Date d = new java.util.Date(); // 컬렉션프레임워크 (개발자에게 유용한 클래스 집합)
//        java.sql.Date d2 = new java.sql.Date(d.getTime()); // jdbc 패키지
//
//       Object [] iArr = {13,3,10,"12", 100, "삼", 50};
//
//       sum=0;


    }
}